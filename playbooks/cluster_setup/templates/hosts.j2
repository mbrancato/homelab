127.0.0.1	localhost

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
{{ ansible_default_ipv4.address }} {{ hostname }}.{{ domain }} {{ hostname }}
{% for node in groups['lab'] %}
{{ hostvars[node]['ansible_default_ipv4']['address'] }} {{ hostvars[node].hostname }}.{{ hostvars[node].domain }} {{ hostvars[node].hostname }}
{% endfor %}
